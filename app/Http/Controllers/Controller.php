<?php

namespace App\Http\Controllers;

/**
 * Clase Base Controller
 * 
 * Clase abstracta que sirve como base para todos los controladores de la aplicación.
 * Todos los controladores heredan de esta clase.
 * 
 * Esta clase está vacía porque Laravel maneja la mayoría de la lógica base
 * a través del framework. Aquí podrías agregar métodos comunes para todos los controladores
 * si fuera necesario (ejemplo: validaciones comunes, autenticación, etc.)
 */
abstract class Controller
{
    // Controladores específicos heredan de esta clase base
}
